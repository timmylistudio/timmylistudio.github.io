# Version 1.2

Last updated: 2026.05

## Updates

- SEO: rewrote `<title>` and meta description with primary identity keywords (`Timmy Li`, `Anqi Li`, Michigan Ross MBA, privacy, AI security).
- SEO: added canonical URL, `robots` meta, and Open Graph + Twitter Card tags with profile image.
- SEO: added JSON-LD structured data for `Person` and `WebSite`, including `alternateName`, `alumniOf`, `hasCredential`, `knowsAbout`, and `sameAs` links to GitHub, LinkedIn, Weibo, and Michigan Ross.
- SEO: synced the static fallback HTML body so crawlers and no-JS previews see real bio, interests, projects, and profile photo immediately, without waiting for `content.json` to load.
- SEO: added `sitemap.xml` and `robots.txt` at the site root; robots disallows `/admin/` and points to the sitemap.
- Content: expanded `site.description`, added name variants (`Timmy Li`, `Anqi Li`, `Timmy Anqi Li`) to bio, and set a descriptive `photoAlt`.
- Content: bumped displayed version to `1.2`.

## Changed Files In Latest Update

- `index.html`
- `content.json`
- `sitemap.xml` (new)
- `robots.txt` (new)
- `Version/v1.2.md` (new)

## Non-Code Follow-Ups

- Verify the site in Google Search Console and submit `https://timmylistudio.github.io/sitemap.xml`.
- Request indexing for `https://timmylistudio.github.io/` after deploy.
- Ensure LinkedIn, GitHub profile, Michigan Ross ambassador page, and project pages link back to the homepage using the name `Timmy Li` where appropriate.
